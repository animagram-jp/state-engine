# view.yml — calendar display state (partial state on browser)

current_date:
  _store:
    client: InMemory
    key: "view.current_date"
  _state:
    type: string

current_mode:
  _store:
    client: InMemory
    key: "view.current_mode"
  _state:
    type: string   # month | week | day
